{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "run-server": "json-server -p 4000 --watch ./data/db.json"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.1.3",
    "nuxt": "3.0.0-rc.13"
  },
  "dependencies": {
    "@pinia/nuxt": "^0.4.3"
  }
}
